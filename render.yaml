services:
  - type: web
    name: LeoApp
    env: php
    plan: free
    buildCommand: ""  # PHP no requiere un comando de compilación
    startCommand: php -S 0.0.0.0:8080 -t public
    envVars:
      - key: ENVIRONMENT
        value: production
